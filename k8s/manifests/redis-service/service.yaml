apiVersion: v1
kind: Service
metadata:
  name: redis-service
  labels:
    app: redis
spec:
  type: ClusterIP
  seLector:
    app: redis
  ports:
    - protocol: TCP
      port: 6379
      targetPort: 6379